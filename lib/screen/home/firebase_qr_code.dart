/*import 'package:flutter/material.dart';
import 'package:qr_flutter/qr_flutter.dart';

class FirebaseQRCode extends StatelessWidget {
  final String firebaseCollectionData;

  FirebaseQRCode(this.firebaseCollectionData);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('QR Code'),
      ),
      body: Center(
        child: QrImageView(
          data: firebaseCollectionData,
          version: QrVersions.auto,
          size: 200.0,
        ),
      ),
    );
  }
}*/
